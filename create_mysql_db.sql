-- Create the database for E-commerce project
CREATE DATABASE IF NOT EXISTS ecommerce_data CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Show databases to confirm
SHOW DATABASES;

-- Use the database
USE ecommerce_data;

-- Show that it's empty (no tables yet)
SHOW TABLES;
